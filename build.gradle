apply plugin: 'idea'
//and standard one
apply plugin: 'java'

sourceSets{
    main{
        java{
            srcDir 'src/main'
        }
    }
    test {
        java {
            srcDir 'src/test'
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
//    library 'org.apache.commons'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
